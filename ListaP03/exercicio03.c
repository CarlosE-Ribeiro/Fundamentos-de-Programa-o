//declaração de bibliotecas
#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

/*Faça um programa que leia um arquivo de texto contendo uma sequencia de números, o primeiro valor
representa o tamanho da sequencia (quantidade de elementos) e salve em um vetor alocado dinamicamente e retorne esse vetor.*/

//função main
int main(){
    //para poder utilizar pontuações
    setlocale(LC_ALL, "");
//declaração de variáveis
FILE* arquivo

arquivo = fopen("sequencia.txt", "w")

if(arq == NULL){
 printf("Erro ao abrir o arquivo!\n");
 exit(1);// 1 significa erro
 }// if


return 0;
}//ma
